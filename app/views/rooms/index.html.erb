<h1>メッセージ一覧</h1>

<% if @rooms.any? %>
  <ul>
    <% @rooms.each do |room| %>
      <li>
        <% other_user = room.users.where.not(id: current_user.id).first %>
        <%= link_to other_user.name, room %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>まだメッセージがありません</p>
<% end %>